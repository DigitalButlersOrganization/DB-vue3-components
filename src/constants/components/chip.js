import { SIZE_MD, SIZE_SM, SIZE_LG } from '../sizes';
import { TYPE_OUTLINE, TYPE_SOLID } from '../types';

export const CHIP = {
	SIZES: [SIZE_MD, SIZE_SM, SIZE_LG],
	TYPES: [TYPE_OUTLINE, TYPE_SOLID],
};
